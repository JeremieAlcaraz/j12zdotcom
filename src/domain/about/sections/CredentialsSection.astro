---
import Badge from '@/ui/atoms/Badge.astro'
import Checkbox from '@/ui/atoms/Checkbox.astro'
import { markdownify } from '@/utils/textConverter'
import { getEntry } from 'astro:content'

interface Section {
  title: string
  content: string
}

const aboutEntry = await getEntry('about', 'index')
if (!aboutEntry) {
  throw new Error('About entry not found! Check src/content/about/index.mdx')
}

const { credentials } = aboutEntry.data as { credentials: Section }
---

<section class="section-sm">
  <div class="container">
    <div class="mx-auto max-w-6xl">
      <!-- Header centré avec badges -->
      <div class="mb-12 text-center lg:mb-16">
        <div class="relative inline-block">
          <h2 class="text-3xl font-bold leading-tight text-base-content md:text-4xl lg:text-5xl">
            {credentials.title}
          </h2>
          <!-- Floating badges autour du titre -->
          <div class="absolute -top-4 -left-8 hidden md:block">
            <Badge label="✨ Reconnu" variant="primary" />
          </div>
          <div class="absolute -top-4 -right-8 hidden md:block">
            <Badge label="🏆 Certifié" variant="secondary" />
          </div>
        </div>

        <p class="mt-6 text-lg text-base-content/70 max-w-3xl mx-auto">
          Des <strong class="text-primary">preuves concrètes</strong> de mon expertise en productivité douce et organisation.
          Parce que la confiance se mérite ! 💜
        </p>
      </div>

      <!-- Grid des accomplissements -->
      <div class="grid grid-cols-1 lg:grid-cols-3 gap-8 mb-12">
        <!-- Notion Ambassador -->
        <div class="group relative p-8 rounded-2xl bg-gradient-to-br from-primary/10 to-primary/5 border border-primary/20 hover:border-primary/30 transition-all duration-300 hover:scale-105">
          <div class="absolute -top-3 -right-3">
            <Badge label="Officiel" variant="primary" />
          </div>

          <div class="text-center space-y-4">
            <div class="text-4xl">📝</div>
            <h3 class="text-xl font-bold text-primary">Notion Ambassador</h3>
            <p class="text-base-content/80 text-sm">
              Reconnu par Notion comme expert et ambassadeur depuis <strong>2021</strong>
            </p>
            <div class="flex justify-center">
              <Checkbox checked disabled color="primary" size="sm" />
            </div>
          </div>
        </div>

        <!-- Diplôme Informatique -->
        <div class="group relative p-8 rounded-2xl bg-gradient-to-br from-secondary/10 to-secondary/5 border border-secondary/20 hover:border-secondary/30 transition-all duration-300 hover:scale-105">
          <div class="absolute -top-3 -right-3">
            <Badge label="Académique" variant="secondary" />
          </div>

          <div class="text-center space-y-4">
            <div class="text-4xl">🎓</div>
            <h3 class="text-xl font-bold text-secondary">Diplômé Informatique</h3>
            <p class="text-base-content/80 text-sm">
              Formation technique solide pour créer des <strong>solutions innovantes</strong>
            </p>
            <div class="flex justify-center">
              <Checkbox checked disabled color="secondary" size="sm" />
            </div>
          </div>
        </div>

        <!-- CV Disponible -->
        <div class="group relative p-8 rounded-2xl bg-gradient-to-br from-accent/10 to-accent/5 border border-accent/20 hover:border-accent/30 transition-all duration-300 hover:scale-105">
          <div class="absolute -top-3 -right-3">
            <Badge label="Transparent" variant="neutral" />
          </div>

          <div class="text-center space-y-4">
            <div class="text-4xl">📄</div>
            <h3 class="text-xl font-bold text-accent">CV Complet</h3>
            <p class="text-base-content/80 text-sm">
              Toute mon expérience en <strong>détail et transparent</strong>
            </p>
            <div class="flex justify-center">
              <a
                href="/docs/CV-Jeremie.pdf"
                target="_blank"
                rel="noopener"
                class="btn btn-sm btn-accent"
              >
                Télécharger
              </a>
            </div>
          </div>
        </div>
      </div>

      <!-- Statistiques impressionnantes -->
      <div class="mb-12">
        <h3 class="text-2xl font-bold text-center mb-8 text-base-content">
          En chiffres 📊
        </h3>

        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
          <div class="text-center p-6 rounded-xl bg-base-200/50 backdrop-blur-sm">
            <div class="text-3xl font-bold text-primary mb-2">500+</div>
            <div class="text-sm text-base-content/70">Personnes accompagnées</div>
          </div>

          <div class="text-center p-6 rounded-xl bg-base-200/50 backdrop-blur-sm">
            <div class="text-3xl font-bold text-secondary mb-2">3+</div>
            <div class="text-sm text-base-content/70">Années d'expertise Notion</div>
          </div>

          <div class="text-center p-6 rounded-xl bg-base-200/50 backdrop-blur-sm">
            <div class="text-3xl font-bold text-accent mb-2">95%</div>
            <div class="text-sm text-base-content/70">Clients satisfaits</div>
          </div>

          <div class="text-center p-6 rounded-xl bg-base-200/50 backdrop-blur-sm">
            <div class="text-3xl font-bold text-success mb-2">0</div>
            <div class="text-sm text-base-content/70">Stress ajouté ! 😌</div>
          </div>
        </div>
      </div>

      <!-- Compétences spécialisées -->
      <div class="mb-12">
        <h3 class="text-2xl font-bold text-center mb-8 text-base-content">
          Mes super-pouvoirs 💪
        </h3>

        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
          <div class="space-y-4">
            <h4 class="font-semibold text-lg text-base-content mb-4">🛠️ Compétences techniques</h4>

            <div class="space-y-3">
              <div class="flex items-center space-x-3">
                <Checkbox checked disabled color="primary" size="sm" />
                <span class="text-base-content/80">Expert Notion (API, Automations, Bases de données)</span>
              </div>

              <div class="flex items-center space-x-3">
                <Checkbox checked disabled color="secondary" size="sm" />
                <span class="text-base-content/80">Conception d'workflows personnalisés</span>
              </div>

              <div class="flex items-center space-x-3">
                <Checkbox checked disabled color="accent" size="sm" />
                <span class="text-base-content/80">Intégrations No-code/Low-code</span>
              </div>

              <div class="flex items-center space-x-3">
                <Checkbox checked disabled color="success" size="sm" />
                <span class="text-base-content/80">Architecture de l'information</span>
              </div>
            </div>
          </div>

          <div class="space-y-4">
            <h4 class="font-semibold text-lg text-base-content mb-4">🧠 Compétences humaines</h4>

            <div class="space-y-3">
              <div class="flex items-center space-x-3">
                <Checkbox checked disabled color="primary" size="sm" />
                <span class="text-base-content/80">Écoute active et empathie</span>
              </div>

              <div class="flex items-center space-x-3">
                <Checkbox checked disabled color="secondary" size="sm" />
                <span class="text-base-content/80">Pédagogie adaptée à chaque profil</span>
              </div>

              <div class="flex items-center space-x-3">
                <Checkbox checked disabled color="accent" size="sm" />
                <span class="text-base-content/80">Accompagnement au changement</span>
              </div>

              <div class="flex items-center space-x-3">
                <Checkbox checked disabled color="success" size="sm" />
                <span class="text-base-content/80">Créativité et innovation</span>
              </div>
            </div>
          </div>
        </div>
      </div>

      <!-- Certifications et formations -->
      <div class="relative">
        <div class="rounded-2xl bg-gradient-to-r from-primary/5 via-secondary/5 to-accent/5 px-8 py-12 backdrop-blur-sm border border-primary/10 md:px-12 md:py-16 lg:px-16 lg:py-20">
          <div class="text-center space-y-6">
            <h3 class="text-2xl font-bold text-base-content">
              Formation continue 📚
            </h3>

            <p class="text-base-content/80 max-w-2xl mx-auto leading-relaxed">
              Je reste à la pointe des <strong>dernières innovations</strong> en productivité et outils numériques.
              Parce que pour vous accompagner au mieux, je dois constamment évoluer ! ✨
            </p>

            <div class="flex flex-wrap justify-center gap-3 mt-6">
              <Badge label="🚀 Innovation" variant="primary" />
              <Badge label="🔄 Veille technologique" variant="secondary" />
              <Badge label="📖 Auto-formation" variant="neutral" />
              <Badge label="🤝 Communauté" variant="primary" />
            </div>

            <!-- Quote inspirante -->
            <div class="mt-8 p-6 rounded-xl bg-base-200/30">
              <blockquote class="text-base italic text-base-content/80">
                "L'expertise sans l'humilité, c'est juste de la suffisance.
                Je continue d'apprendre chaque jour, notamment grâce à mes clients !"
              </blockquote>
              <footer class="mt-3 text-sm text-base-content/60">
                — Ma philosophie d'apprentissage 💜
              </footer>
            </div>
          </div>
        </div>

        <!-- Decorative elements -->
        <div class="absolute -inset-2 -z-10 rounded-3xl bg-gradient-to-br from-primary/5 to-secondary/5 blur-xl"></div>

        <!-- Corner accents -->
        <div class="absolute -left-2 -top-2 h-8 w-8 rounded-full bg-gradient-to-br from-primary/20 to-transparent"></div>
        <div class="absolute -bottom-2 -right-2 h-12 w-12 rounded-full bg-gradient-to-tl from-secondary/20 to-transparent"></div>
        <div class="absolute top-1/4 -right-4 h-6 w-6 rounded-full bg-gradient-to-tl from-accent/30 to-transparent"></div>
      </div>
    </div>
  </div>
</section>
