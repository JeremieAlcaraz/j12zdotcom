---
import Card from '@/ui/organisms/Card.astro'
import EquipmentImage from '@/domain/equipment/components/EquipmentImage.astro'
import PriceTag from '@/domain/equipment/components/PriceTag.astro'
import type { CollectionEntry } from 'astro:content'

interface Props {
  item: CollectionEntry<'shop'>
}

const { item } = Astro.props as Props
const {
  title,
  description,
  summary,
  cover,
  price,
  priceType,
  currency,
  type,
  productType,
  serviceType,
  categories = [],
  isNew,
} = item.data

const href = `/equipment/${item.id}`
const isFree = price === 0
const typeLabel = type === 'product' ? 'Produit' : 'Service'
const subtype = productType ?? serviceType ?? ''

const categoryLabels: Record<string, string> = {
  notion: 'Notion',
  automatisation: 'Automatisation',
  ai: 'AI',
  sante: 'Sante',
  'prendre-soin': 'Prendre soin',
}

const subtypeLabels: Record<string, string> = {
  'templates-notion': 'Templates Notion',
  musiques: 'Musiques',
  prompts: 'Prompts',
  divers: 'Divers',
  formation: 'Formation',
  coaching: 'Coaching',
  ateliers: 'Ateliers',
}

const displayCategories = categories.map((category) => categoryLabels[category] ?? category)
const subtypeLabel = subtype ? subtypeLabels[subtype] ?? subtype : ''
const dataType = type === 'product' ? 'products' : 'services'
const dataCategories = categories.join('|')
---

<article
  class="shop-card"
  data-shop-item
  data-type={dataType}
  data-subtype={subtype}
  data-categories={dataCategories}
  data-price={price}
  data-is-new={isNew ? 'true' : 'false'}
>
  <Card class="flex h-full flex-col" base={100} shadow="sm" hover>
    {
      cover && (
        <div slot="image" class="aspect-square w-full overflow-hidden">
          <a href={href} class="block" aria-label={`Voir ${title}`}>
            <EquipmentImage
              class="h-full w-full object-cover"
              src={cover}
              alt={title}
              width={640}
              height={640}
              format="webp"
            />
          </a>
        </div>
      )
    }
    <a slot="title" href={href} class="link link-hover line-clamp-2">
      {title}
    </a>
    <p class="text-sm text-base-content/70 line-clamp-3">
      {summary || description}
    </p>
    <div class="mt-4 flex items-center justify-between">
      <PriceTag price={price} priceType={priceType} currency={currency} isFree={isFree} />
      <a href={href} class="btn btn-primary btn-sm">Voir</a>
    </div>
  </Card>
</article>
