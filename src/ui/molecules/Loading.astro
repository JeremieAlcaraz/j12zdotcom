---
import Loader from '@/ui/atoms/Loader.astro'
import Section from '@/ui/atoms/Section.astro'

interface Props {
  text?: string
}
const { text = 'Chargement...' } = Astro.props as Props
---

<Section class="flex items-center justify-center space-x-2">
  <Loader variant="ring" />
  <span>{text}</span>
</Section>
