---
import { Image } from 'astro:assets';
import heroAvif from '@assets/img_opt/jeremie-hero.avif';
import heroWebp from '@assets/img_opt/jeremie-hero.webp';
import heroPng from '@assets/img_opt/jeremie-hero.png';
---

<!--  
  Section “hero” pleine hauteur de viewport moins 4 rem (taille de ta barre)  
  - bg-base-200 : couleur de fond (thème)  
  - h-[calc(100vh-4rem)] : calcule 100 % de la hauteur de la fenêtre – 4 rem  
  - p-25 : padding autour du conteneur  
-->
<div class="hero bg-base-200 h-[calc(100vh-4rem)] p-25">
  <!--  
    Contenu interne en flex :  
    - flex-col par défaut (petits écrans)  
    - lg:flex-row sur écrans larges  
    - gap-12 : espacement entre image et texte  
    - max-w-full, px-0 pour contrôler la largeur/padding  
  -->
  <div class="hero-content flex-col lg:flex-row max-w-full px-0 gap-12">
    <!--  
      Balise <picture> pour sources multiples :  
      - AVIF puis WebP, sinon fallback PNG  
      - classes Tailwind pour style (coins arrondis, ombre, découpe overflow)  
    -->
    <picture class="max-w-lg lg:max-w-xl rounded-lg overflow-hidden">
      <!-- Source AVIF (meilleur ratio compression) -->
      <source srcset={heroAvif.src} type="image/avif" />
      <!-- Source WebP (fallback si AVIF non supporté) -->
      <source srcset={heroWebp.src} type="image/webp" />
      <!--  
        Composant <Image> d’Astro :  
        - src : image PNG de secours  
        - alt : accessibilité  
        - width/height : dimensions de rendu  
        - loading="eager" : chargement immédiat  
      -->
      <Image src={heroPng} alt="Portrait de Jérémie" width={600} height={600} loading="eager" />
    </picture>

    <!--  
      Bloc texte  
      - px-4 sur petits écrans, lg:px-8 sur grands  
    -->
    <div class="px-4 lg:px-8">
      <!-- Titre principal large et en gras -->
      <h1 class="text-6xl font-bold mb-4">Box Office News!</h1>

      <!-- Paragraphe avec spacing et interligne confortable -->
      <p class="py-6 text-lg leading-relaxed">
        Provident cupiditate voluptatem et in. Quaerat fugiat ut assumenda excepturi exercitationem
        quasi. In deleniti eaque aut repudiandae et a id nisi.
      </p>

      <!-- Bouton d’appel à l’action, style “primary” en grand -->
      <button class="btn btn-primary btn-lg px-8 py-3"> Get Started </button>
    </div>
  </div>
</div>
