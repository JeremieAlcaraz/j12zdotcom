---
interface Props {
  class?: string;
}

const { class: className = '', ...attrs } = Astro.props as Props;
---
<section class={`py-8 sm:py-12 lg:py-16 ${className}`} {...attrs}>
  <slot />
</section>
