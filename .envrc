# Automatically load the Nix development environment when entering this directory
# Requires: https://direnv.net/

if ! has nix_direnv_version || ! nix_direnv_version 3.0.4; then
  source_url "https://raw.githubusercontent.com/nix-community/nix-direnv/3.0.4/direnvrc" "sha256-DzlYZ33mWF/Gs8DDeyjr8mnVmQGx7ASYqA5WlxwvBG4="
fi

# Use flake
use flake

# Load encrypted secrets for development
if [ -f secrets/dev.enc.env ]; then
  export $(sops -d secrets/dev.enc.env | grep -v '^#' | grep -v '^$' | xargs)
  echo "🔓 Secrets de dev chargés"
fi

# Display welcome message
echo "🚀 Nix development environment loaded!"
echo "Node.js: $(node --version)"
echo "pnpm: $(pnpm --version)"
